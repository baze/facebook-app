{{--<div ng-hide="user.id" class="text-center">--}}
    <p>
        <fb-login label="Klick hier, um mitzuspielen"
                  class="{{ isset($classes) ? $classes : '' }}"
                  redirect="true">
        </fb-login>
    </p>
{{--</div>--}}